<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.Healthcareplatform.location.mapper.ProfessionalProfileGeoMapper">

    <select id="findNearby" resultType="org.example.Healthcareplatform.location.dto.NearbyProfessionalRow">
        SELECT id,
               user_id,
               first_name,
               last_name,
               title,
               specialty,
               bio,
               phone,
               address,
               city,
               latitude,
               longitude,
               active,
               created_at,
               updated_at,
               (6371 * ACOS(
                   COS(RADIANS(#{lat})) * COS(RADIANS(latitude)) *
                   COS(RADIANS(longitude) - RADIANS(#{lng})) +
                   SIN(RADIANS(#{lat})) * SIN(RADIANS(latitude))
               )) AS distance_km
        FROM professional_profiles
        WHERE active = TRUE
          AND latitude BETWEEN #{lat} - (#{radiusKm} / 111.0) AND #{lat} + (#{radiusKm} / 111.0)
          AND longitude BETWEEN #{lng} - (#{radiusKm} / (111.0 * COS(RADIANS(#{lat})))) 
                               AND #{lng} + (#{radiusKm} / (111.0 * COS(RADIANS(#{lat}))))
          AND (#{specialty} IS NULL OR LOWER(specialty) LIKE CONCAT('%', LOWER(#{specialty}), '%'))
        HAVING distance_km &lt;= #{radiusKm}
        ORDER BY distance_km ASC
    </select>

</mapper>
